[tox]
requires = tox>=4
envlist = py310,py311,py312,py313,py314

[testenv]
# Dependencies now come from pyproject.toml (runtime deps + the "test" extra)
# rather than a separate requirements.txt.
extras = test
commands = pytest