.. This file is a pointer, not a copy. The change log lives in CHANGELOG.rst at
   the repository root; including it here keeps the published docs from drifting
   out of date, which is what happened previously (this copy had stopped at
   v0.5.2 while the root file was three releases ahead).

.. include:: ../CHANGELOG.rst
